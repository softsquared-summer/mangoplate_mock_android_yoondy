<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/search_suggest_iv_search"
        android:layout_width="wrap_content"
        android:layout_height="15dp"
        android:tint="@color/middleGray"
        android:adjustViewBounds="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="14dp"
        android:layout_marginTop="17dp"
        android:layout_marginBottom="17dp"
        android:layout_marginEnd="14dp"
        android:src="@drawable/ic_search"/>

    <TextView
        android:id="@+id/search_suggest_tv_search_word"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="@+id/search_suggest_iv_search"
        app:layout_constraintBottom_toBottomOf="@+id/search_suggest_iv_search"
        app:layout_constraintStart_toEndOf="@+id/search_suggest_iv_search"
        android:layout_marginStart="15dp"
        android:textColor="@android:color/black"
        android:text="검색어"/>

</androidx.constraintlayout.widget.ConstraintLayout>